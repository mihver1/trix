# Chat 202603261111
## User
Пройтись по текущему FFI/core ядру `trix-core`, проактивно поискать баги на рассинхрон local/server state, перепроверить всё трижды и в конце дать чёткий отчёт: что проверено, что найдено, что изменено.
## Assistant
Провёл картирование `trix-core`, FFI-границ, sync/persistence/realtime/dataflow, поднял baseline и исследовал основные модули:
- `crates/trix-core/src/sync.rs`
- `crates/trix-core/src/storage.rs`
- `crates/trix-core/src/realtime.rs`
- `crates/trix-core/src/ffi.rs`
- `crates/trix-core/src/messenger.rs`
- серверные контракты в `crates/trix-server`
## Assistant
Зафиксировал baseline:
- `cargo test -p trix-core` был зелёным до правок
- unit tests: 70 passed
- `ffi_surface_test`: 29 passed
- postgres-зависимые e2e были ignored
## Assistant
По результатам нескольких проходов аудита подтвердил два класса проблем:
1. Recovery/bootstrap bug:
   - при потере local bootstrap/MLS mapping и уже ушедшем sync cursor ядро могло не скачать commit/welcome из ранней истории и застрять в состоянии `chat has no bootstrappable MLS state`
   - тот же паттерн был в safe messenger path
2. FFI lock/ordering bug:
   - `FfiRealtimeDriver::next_websocket_event` раньше держал `websocket` + `coordinator` + `store` во время блокирующего ожидания frame
   - несколько методов `FfiLocalHistoryStore` брали `facade` раньше `store`, что конфликтовало с потоками, где порядок был `store -> facade`
## User
ап
## Assistant
Внёс исправления:
- `crates/trix-core/src/sync.rs`
  - `prepare_chat_mutation_conversation` теперь при `needs_bootstrap` всегда делает full history fetch (`get_chat_history(chat_id, None, None)`)
- `crates/trix-core/src/messenger.rs`
  - `bootstrap_chat_if_needed` также переведён на full history fetch при отсутствии local bootstrap state
  - удалён устаревший `after_server_seq` из call sites
- `crates/trix-core/src/ffi.rs`
  - `FfiRealtimeDriver::next_websocket_event` больше не удерживает `store`/`sync` во время ожидания websocket frame
  - store/facade lock ordering выровнен на `store -> facade`
## Assistant
Добавил регрессионные тесты:
- `sync::tests::send_message_bootstraps_from_full_history_when_cursor_is_ahead_and_local_bootstrap_is_missing`
- `ffi::tests::project_chat_with_facade_waits_on_store_before_locking_facade`
- `ffi::tests::load_or_bootstrap_chat_conversation_waits_on_store_before_locking_facade`
- `ffi::tests::next_websocket_event_does_not_lock_store_or_sync_while_waiting_for_a_frame`
## Assistant
Прогнал проверки после правок:
- таргетированные регрессии — зелёные
- `cargo test -p trix-core` — зелёный
  - main crate tests: 74 passed
  - `ffi_surface_test`: 29 passed
  - postgres-dependent e2e (`client_scenario_e2e`, `control_e2e`, `safe_ffi_e2e`) остались ignored из-за окружения
## Assistant
Итог:
- подтверждённые баги исправлены
- recovery-path и FFI locking покрыты регрессионными тестами
- дополнительных подтверждённых рассинхронов в проверенных dataflows не найдено
- абсолютную гарантию `bullet-proof` без запуска postgres e2e и дальнейшего production soak-testing давать нельзя, но по проверенным потокам найденные реальные риски закрыты
