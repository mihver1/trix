# Local Certificate Mount

Put deployment-local TLS certificates here only for local testing.

Do not commit private keys, real certificates, APNs keys, account passwords, or
other secrets. A production deployment should mount certificates from a host
secret path or reverse-proxy-managed certificate store.
